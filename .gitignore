# Dependencies (app runtime deps only; the vendored Node toolchain in tools/node is tracked)
/node_modules

# Secrets - never commit
.env
**/.env

# OS / editor noise
.DS_Store

# Scratch output
/screenshots/
output/
.gstack/
3d-models/
/Screenshot*.png
